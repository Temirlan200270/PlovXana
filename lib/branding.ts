/**
 * Единственный источник правды для отображаемого имени бренда на публичном сайте.
 *
 * Канон: **ИП АБИШЕВ** — оператор сервиса обработки заказов для кафе
 * (верификация Meta: реальный бизнес-сервис, а не сайт заведения).
 */
export const BRAND_NAME = "ИП АБИШЕВ" as const;

/** Слоган: сервис, не «платформа ради API». */
export const BRAND_TAGLINE = "Ускорение обработки заказов в кафе" as const;
