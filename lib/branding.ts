/**
 * Единственный источник правды для отображаемого имени бренда.
 *
 * Канон для публичного сайта и юр. страниц: **PlovXana PVL** (латиница + регистр X).
 *
 * Любое изменение — только здесь. UI, SEO-метаданные, пилотный tenant row,
 * тесты — все импортируют эту константу, чтобы не плодить расхождения вроде
 * `PLOVXANA` / `Plovxana` / кириллических вариантов в шапке и метаданных.
 */
export const BRAND_NAME = "PlovXana PVL" as const;

/**
 * Слоган для копирайта футера. Пустой `HERITAGE CUISINE` выглядел чужеродно
 * при русскоязычном сайте — перевёл и оставил каноничным единым словом.
 */
export const BRAND_TAGLINE = "НАСЛЕДИЕ КУХНИ" as const;
