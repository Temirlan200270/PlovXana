# Meta verification — данные для заполнения

Этот файл — чеклист, чтобы быстро и без ошибок пройти Business Verification / API review.

## 1) Что вводить в Meta

- **Company name**: `ИП Абишев` (как в документах)
- **Website**: `plovxanapvl.com`
- **Business email**: `info@plovxanapvl.com`

## 2) Что Meta должна увидеть на сайте

- Упоминание связки **бренд ↔ юр.лицо**: `Plovxana PVL` + `ИП Абишев`
- Контактный email **на домене**: `@plovxanapvl.com`
- Город/страна: Павлодар, Казахстан
- Публичные policy-страницы:
  - `/privacy`
  - `/terms`

## 3) Какие значения нужно подставить (реквизиты)

Заполни и передай эти значения — они должны совпадать с документами.

- **БИН/ИИН**: `830520350723`
- **Юридический адрес (одной строкой)**: `Казахстан, г. Павлодар, ул. Естая, д. 81/1`
- **Контактный телефон (желательно)**: `+77011000570`

## 4) Где это настраивается в проекте

Проект поддерживает подстановку реквизитов через env-переменные (на VPS/CI).

Обязательные (можно оставить по умолчанию, но лучше явно задать):

- `LEGAL_BRAND_NAME` (пример: `Plovxana PVL`)
- `LEGAL_OPERATOR_NAME_RU` (пример: `ИП Абишев`)
- `LEGAL_OPERATOR_NAME_EN` (пример: `Individual Entrepreneur Abishev (Kazakhstan)`)
- `LEGAL_DOMAIN` (пример: `plovxanapvl.com`)
- `LEGAL_PUBLIC_EMAIL` (пример: `info@plovxanapvl.com`)
- `LEGAL_CITY` (пример: `Павлодар`)
- `LEGAL_COUNTRY` (пример: `Казахстан`)
- `LEGAL_ADDRESS_LINE` (пример: `Казахстан, г. Павлодар, ул. Естая, д. 81/1`)

Рекомендуемые:

- `LEGAL_IIN_BIN` (БИН/ИИН, пример: `830520350723`)
- `LEGAL_PUBLIC_PHONE` (пример: `+77011000570`)

